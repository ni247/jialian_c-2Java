package cn.itcast.demo01;

/*
 *   导入包的话 ,新增加一个文件夹 lib
 *   将jar文件导入过来.
 *   右键build path
 *   
 *   不同修饰符使用细节
 *   成员包含成员变量,成员函数
 *   public,修饰类,成员
 *   protect .修饰成员
 *   默认不写 修饰类,成员
 *   private 修饰成员
 *   static 修饰成员
 *   final 最终修饰符,修饰类,方法,成员,局部成员
 *   
 *   修饰构造方法能够使用的修饰符
 *   publc 公共的
 *   protect 受保护
 *    默认
 *    private  私有的.
 */
//import cn.itcast.sum.GetAvg;

public class Test {
	public static void main(String[] args) {
		// double a = GetAvg.getAvg(1, 2);
	}
}
